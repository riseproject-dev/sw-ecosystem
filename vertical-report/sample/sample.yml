title: "Sample -- RISC-V Ecosystem Status"
target_profile: RVA23U64
hardware_label: "Hardware: RISC-V CPU (RVA23U64)"
columns: [GCC, LLVM]
product_layers: ["Toolchains"]
shared_layers: ["System Libraries"]
legend:
  - {color: green,  label: "upstream builds+tests+releases; optimized"}
  - {color: blue,   label: "upstream builds+tests; mostly optimized"}
  - {color: yellow, label: "upstream builds; some optimized"}
  - {color: orange, label: "no upstream build, distributions only; no optimizations"}
  - {color: red,    label: "not working"}
  - {color: grey,   label: "unknown or N/A"}
nodes:
  - name: gcc
    color: yellow
    criticality: critical
    column: GCC
    layer: "Toolchains"
    release_provider: Debian
    upstream_release: false
    gap: "No upstream riscv64 Buildbot builders; clean Debian/Ubuntu/Arch builds from unpatched GCC 14+"
  - name: LLVM
    color: blue
    criticality: critical
    column: LLVM
    layer: "Toolchains"
    release_provider: Debian
    upstream_release: false
    gap: "RISE-sponsored CI passes check-all+test-suite via QEMU; no riscv64 binary in upstream releases"
  - name: glibc
    color: yellow
    criticality: critical
    column: null
    layer: "System Libraries"
    release_provider: Debian
    upstream_release: false
    gap: "Both upstream Buildbot builders offline since mid-2025; RVV string suite (18 routines) not merged; open SIGILL bug in merged RVV memset"
